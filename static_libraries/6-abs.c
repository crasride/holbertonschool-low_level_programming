#include "main.h"
/**
* _abs - function
* @n: operand
* Description:  function that computes the absolute value of an integer.
* Return: Always 0 (Success)
*/
int _abs(int n)
{
	if (n < 0)
	{
	return (n * -1);
	}
	return (n);
}
